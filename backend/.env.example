# Backend environment variables (DO NOT COMMIT real secrets)
# Copy to .env and fill with real values

# Server
PORT=4000
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000

# Database (NEVER expose to frontend!)
DB_HOST=gateway01.eu-central-1.prod.aws.tidbcloud.com
DB_PORT=4000
DB_USER=3YTK6Em4WhtFiqF.root
DB_PASSWORD=ovAH3n3bu2YabeK0
DB_DATABASE=oshxona_erp_v2
DB_CONNECTION_LIMIT=10
DB_QUEUE_LIMIT=20

# Auth
JWT_SECRET=change-me-to-a-strong-random-string-at-least-64-chars
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=7d
JWT_ISSUER=restoran-pos-v2
BCRYPT_COST=10

# Rate limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=200

# Logging
LOG_LEVEL=info
